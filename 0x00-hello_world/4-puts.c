#include <stdio.h>
/**
 * main -entry point of a program where the execution of a program starts
 *
 * Return: Always end the execution of a function(0)
 *
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
