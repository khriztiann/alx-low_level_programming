#include "main.h"

/**
 * _islower - print 1 for lower and 0 for otherwise
 * @c : the character to be checked
 * Return: return 1 for lowercase characters and 0 if otherwise
 */

int _islower(int c)
{

			if (c >= 97 && c <= 122)
			{
				return (1);
			}
				return (0);
}
